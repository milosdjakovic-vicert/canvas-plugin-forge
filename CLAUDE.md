# Plugin Forge — Project Instructions

Canvas plugin development workspace.

## Structure

- `plugins/` — Active plugin projects (untracked, each manages its own repo)
- `misc/` — Fixtures, POCs, example documents (tracked)

## Skills

- `/commit` — Git commits following project conventions (global skill at `~/.claude/skills/commit/`)
